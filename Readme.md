### Playwright

e2e testing suite using https://playwright.dev/ 

#### Running tests

For the time being you need to supply a username and password in an env file:
* copy `.env.example` and rename to `.env`
* enter a valid username & password

```npx playwright test --ui```

